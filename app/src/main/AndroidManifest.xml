<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.anubhav.assignmentsfeed">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LRContainerActivity" />
        <activity android:name=".DetailsActivity" />
        <activity android:name=".PersonProfileActivity" />
        <activity android:name=".ImageViewerActivity" />
        <activity android:name=".EditPersonProfileActivity" />
        <activity android:name=".AddAssignmentActivity" />
        <activity android:name=".BasicCredentialsActivity" />
        <activity android:name=".StudentOrProfessorActivity" />
        <activity android:name=".SetProfileImageActivity" />
        <activity android:name=".SetupActivity" />

        <activity
            android:name=".OpeningActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".MainActivity" />

        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat" />
    </application>

</manifest>